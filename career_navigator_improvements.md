# 🚀 Career Navigator - 고도화 및 아키텍처 개선 로드맵
> **작성일자:** 2026-05-23
> **목적:** 향후 정교한 AI/ML 추천 모델 개발 및 엔터프라이즈 아키텍처 마이그레이션 시 반영해야 할 핵심 보완 사항 및 최적화(걷어낼 사항) 가이드라인 정리

---

## 1. 🌟 서비스 보완 및 확장 포인트 (To-Be Enhanced)

향후 추천 알고리즘 모델링 및 UI/UX 설계 시 반드시 반영해야 할 핵심 고도화 요소입니다.

### 1.1 직무 전환 비용(Transition Cost) 모델링
* **개념:** 단순히 "갈 수 있는 직무" 추천을 넘어 "그곳으로 가는 데 따르는 현실적 비용과 난이도"를 예측합니다.
* **모델 공식 예시:**  
  $$전환 비용 = f(스킬 갭 개수, 평균 이동 소요 연차, 필요 라이선스/교육 수, 타 조직 이동 여부)$$
* **반영 방안:** 3대 시나리오(안전형/도전형/T자형) 추천 카드 영역에 전환 비용 등급(낮음/보통/높음) 및 준비 기간을 시각적 배지로 표시합니다.

### 1.2 조직 수요(Organization Demand) 융합 매칭
* **개념:** 개인의 역량(Supply) 중심 매칭에서 탈피하여 조직의 실제 수요(Demand)를 정합시킵니다.
* **반영 데이터:** 사내 직무별 실시간 공석(Vacancy) 정보, 해당 조직의 최근 3개년 인원 성장 추세, 신규 채용 빈도.
* **반영 방안:** 갈 수 있는 경로 중 실제로 공석이 있는 직무에 "🟢 즉시 이동 가능" 배지를 붙이고, 수요가 축소되는 직무에는 "⚠️ 경쟁률 높음" 등의 경고를 제공하여 현실적 매칭 신뢰도를 확보합니다.

### 1.3 감정 설계를 고려한 긍정적 프레이밍 (Emotional Design)
* **개념:** 커리어 데이터 분석이 직원에게 박탈감이나 불안감을 주지 않도록 정서적 안전망을 설계합니다.
* **반영 방안:** 
  * "동료 대비 하위 N%"와 같은 서열 중심 표현은 전면 금지합니다.
  * **"비슷한 연차의 동료들이 현재 가장 집중적으로 개발하고 있는 상위 스킬 3가지"** 혹은 **"현재 나의 강점 스킬을 가장 유용하게 살릴 수 있는 방향"** 등 성장 촉진형 긍정 언어로 전환합니다.

### 1.4 커리어 타임라인(Timeline) 구체화
* **개념:** 커리어 패스에 흐르는 시간의 개념을 직관적으로 구조화합니다.
* **반영 방안:** 커리어 그래프에 시간축 슬라이더를 도입하여 "3년 후 나의 예상 위치", "5년 후 목표 도달을 위해 지금 수행해야 할 마일스톤"을 타임라인 흐름도로 제공합니다.

---

## 2. ✂️ 서비스 단순화 및 걷어낼 포인트 (To-Be Pruned)

시스템의 복잡도를 낮추고 데이터 파이프라인의 오류 가능성을 차단하기 위해 걷어내야 할 핵심 항목들입니다.

### 2.1 스킬 ID 체계 이원화 및 접착 코드(Glue Code) 제거
* **현상:** 온보딩 자가 진단(`SK_HR_01` 등)과 추천 교육 리소스(`SKL_HR_002` 등)의 스킬 식별코드가 달라 중간에 맵핑 딕셔너리(`SKILL_MAP`)가 비대하게 존재합니다.
* **개선(걷어낼 점):** 중간의 복잡한 맵핑 접착 코드를 전량 걷어내고, **처음부터 모든 스키마의 스킬 식별코드를 `SKL_XXX_000` 표준 규격으로 일원화**하여 유기적으로 바로 연결되도록 결합합니다.

### 2.2 이중화된 정적 데이터(Split Source of Truth) 통합
* **현상:** `src/data/` 내부의 정제된 JSON 파일군과 `careerData.js` 파일 내 하드코딩된 대규모 정적 객체에 동일 데이터가 중복 보관되고 있습니다.
* **개선(걷어낼 점):** `careerData.js` 내부에 하드코딩된 정적 객체 정의를 완전히 걷어내고, 원본 JSON 파일에서 ESM Import 또는 API 호출을 통해 데이터를 한곳에서만 주입받도록 단일 진실 공급원(Single Source of Truth) 구조로 통합합니다.

### 2.3 과도한 정량적 상대 평가 지표 단순화
* **현상:** '리더십 백분위', '동료 대비 점수' 등 검증하기 어렵고 오해를 낳기 쉬운 상대 평가 지표들이 다수 나열되어 있습니다.
* **개선(걷어낼 점):** 불필요하게 세분화된 정량 지표들을 걷어내고, 사용자가 직관적으로 이해할 수 있는 **‘현재 내 스킬 숙련도(5단계)’**와 **‘목표 직무로 가기 위한 스킬 갭 개수’** 정도로 핵심 지표를 심플하게 축소합니다.

---

## 3. 🏗️ 향후 모델 개발 및 아키텍처 구현 가이드

### 3.1 렌더링 성능 최적화 (Memory & Reference)
* **가이드:** `CareerGraphPage.jsx` 등 Cytoscape 또는 Chart 라이브러리를 바인딩할 때, 스타일시트(Stylesheet)나 레이아웃(Layout) 설정 리터럴이 매 렌더링마다 메모리에 재할당되지 않도록 **함수 컴포넌트 외부(Static Constants)로 분리**하거나 `useMemo`로 묶어 미세 버퍼링을 원천 방지합니다.

### 3.2 거대 모놀리식 파일의 컴포넌트 모듈화
* **가이드:** UI 단계가 복잡한 온보딩 마법사와 대시보드는 단계별 Step 컴포넌트와 세부 카드로 쪼개어 개별 파일 크기를 **200라인 이하**로 분리 제어합니다.

### 3.3 프론트엔드 인라인 스타일의 CSS 클래스 이관
* **가이드:** JSX 내에 인라인으로 흩어진 `style={{ ... }}` 객체들을 전량 제거하고, `index.css`에 글래스모피즘 표준 패널 클래스(예: `.glass-panel`, `.glow-cyan-border`)로 정의하여 스타일시트를 바인딩해 가상 DOM 연산 지연을 줄이고 반응형 미디어 쿼리를 전폭 지원합니다.
